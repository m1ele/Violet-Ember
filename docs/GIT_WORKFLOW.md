# Git workflow

- `main` contains stable releases.
- `dev` contains experimental work.
- Features are developed on short-lived branches.

Recommended flow:

```bash
git checkout dev
git checkout -b feature/accent-engine
```
